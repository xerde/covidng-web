import { AxiosInstance } from './AxiosInstance';
import { covidAPIs } from './index';
//we'll put the api calls here and just reference them in our component
//we'll use the axios instance to make the calls(e.g AxiosInstance.get(covidAPIs.login))
